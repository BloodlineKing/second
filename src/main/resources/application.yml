spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/first?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
server:
  port: 8080